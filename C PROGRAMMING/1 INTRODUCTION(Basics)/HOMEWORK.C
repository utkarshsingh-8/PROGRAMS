#include <stdio.h>

int main()
{
    // Write a program to calculate perimeter of rectangle.Take sides, a & b, from the user.
    /*float a, b;                                                            //😖😒😉
    printf("Enter the value of sidC PROGRAMMING/APNA COLLEGE/HOMEWORK.Ces a and b\n");
    scanf("%f %f", &a, &b);
    printf("Perimeter of rectangle is %f", 2 * (a + b));*/

    // Take a number(n) from user & output its cube(n*n*n).                  //😖😒😉
    /*float n;
    printf("Enter the number:");
    scanf("%f", &n);
    printf("Cube is : %f", (n * n * n));*/

    // Write a program to print the average of 3 numbers.                    //😖😒😉
    /*float x, y, z;
    printf("Enter the value of x,y,z\n");
    scanf("%f %f %f", &x, &y, &z);
    printf("The average of 3 numbers is: %f",(x + y + z) / 3);*/

    // Write a program to check if given character is digit or not.          //😖😒😉
    /* int digit;
     printf("Enter a character:");
     scanf("%d", &digit);
     printf("%d", digit > 0 && digit < 9);   //digit -->1  otherwise -->0*/

    // Write a program to print the smallest number of two.                  //😖😒😉
    /* int x, y, w;
     printf("Enter the value of x and y\n");
     scanf("%d %d", &x, &y);
     w = x > y;
     printf("%d\n", w);  // w==1(y is smallest) || w==0(x is smallest)*/

    // Write a program to check if the given number is a natural number.     //😖😒😉
    /*float f;
    int i;
    printf("Enter the number:");
    scanf("%f", &f);
    i = f;
    if (i > 0 && i == f)
    {
        printf("Entered number is natural number");
    }
    else
    {
        printf("Not natural number");
    }*/

    // Search on what a "nested loop" is & print this pattern using it.      //😖😒😉
    /*char ch = '*';
    for (int i = 1; i < 5; i++)
    {
        for (int j = 1; j < 5; j++)
        {
            printf("%c", ch);
        }
        printf("\n");
    }*/

    

    return 0;
}
