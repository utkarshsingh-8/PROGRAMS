#include <stdio.h>
#include <stdlib.h>

int main()
{
    // WAP TO ALLOCATE MEMORY OF SIZE N WHERE N IS ENTERED BY THEUSER//🤔🤔🤔🤔
    /*int n;
    printf("Enter the value of N :");
    scanf("%d", &n);
    int *ptr;
    ptr = (int *)calloc(n, sizeof(int));
    for (int i = 0; i < n; i++)
    {
        printf("%d\n", ptr[i]);
    }

    // free() WE USE IT TO FREE MEMORY THAT IS ALLOCATED USING MALLOC & CALLOC
    // X                                                             //🤔🤔🤔🤔
    free(ptr);
    ptr = (int *)calloc(3, sizeof(int));
    for (int i = 0; i < 3; i++)
    {
        printf("%d\n", ptr[i]);
    }*/
    
    return 0;
}