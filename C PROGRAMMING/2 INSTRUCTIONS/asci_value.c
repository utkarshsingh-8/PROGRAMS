// Write a Program to Find ASCII Value of a Character
#include <stdio.h>

int main()
{
    char ch;
    printf("Enter any character:\n");
    scanf("%c", &ch);
    printf("The ASCI value of character you entered is %d", ch);

    return 0;
}