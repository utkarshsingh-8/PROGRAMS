#include <stdio.h>

int main()
{

    // WRITE A PROGRAM TO CHECK WHETHER A STUDENT IS PASS OR FAIL  //👻👻👻
    /*int marks;
    printf("Enter marks between (0-100):");
    scanf("%d", &marks);
    if (marks < 30 && marks >= 0)
    {
        printf("Student is fail");
    }
    else if (marks >= 30 && marks < 100)
    {
        printf("Student is pass");
    }
    else
    {
        printf("Wrong marks");
    }*/
    // marks >= 30 ? printf("Pass") : printf("Fail");

    // WRITE A PROGRAM TO GRADE TO STUDENT                         //👻👻👻
    /* int marks;
     printf("Enter the marks between (0-100):");
     scanf("%d", &marks);
     if (marks < 30)
     {
         printf("C");
     }
     else if (marks >= 30 && marks < 70)
     {
         printf("B");
     }
     else if (marks >= 70 && marks < 90)
     {
         printf("A");
     }
     else if (marks >= 90 && marks <= 100)
     {
         printf("A+");
     }
     else
     {
         printf("Entered wrong marks");
     }*/

    // WRITE A PROGRAM TO FIND A CHARACTER ENTERED BY USER IS UPPER CASE OR NOT👻👻👻
    /*char ch;
    printf("Enter a alphabetic character:");
    scanf("%c", &ch);
    //if (ch >= 65 && ch <= 90)
    if(ch >='A' && ch <='Z')
    {
        printf("Upper Case");
    }
    //else if (ch >= 97 && ch <= 122)
    else if(ch >='a' && ch<='z')
    {
        printf("Lower Case");
    }
    else
    {
        printf("Entered Wrong Character");
    }*/

    // OUTPUT IS "ELSE-IF"
    /*float x = 0.1;
    if (x == 0.1)
        printf("IF");
    else if (x == 0.1f)
        printf("ELSE IF");
    else
        printf("ELSE");

    // OUTPUT IS "IF"
    float x = 0.5;
    if (x == 0.5)
        printf("IF");
    else if (x == 0.5f)
        printf("ELSE IF");
    else
        printf("ELSE");*/

    // float x = 0.1;
    // printf("%d %d %d", sizeof(x), sizeof(0.1), sizeof(0.1f));

    return 0;
}