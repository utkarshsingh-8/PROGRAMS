#include <stdio.h>

int main()
{
    // TO CHECK A NUMBER IS PRIME NUMBER OR NOT                         //😒😉🙄
    /*int number, count = 0, result;
    printf("Enter a positive number:");
    scanf("%d", &number);

    for (int i = 2; i < number; i++)
    {
        number % i == 0 ? count = count + i : count == 0;
    }
    count > 1 ? printf("Not Prime Number") : printf("Prime Number");*/

    // WRITE A PROGRAM TO PRINT PRIME NUMBER IN A RANGE                 //😉🙄🙂
    // n1=lower_range, n2=upper_range
    /*int n1, n2, i, j, count;
    printf("Enter the value of n1 and n2\n");
    scanf("%d %d", &n1, &n2);
    printf("The prime number between %d and %d\n", n1, n2);
    for (i = n1; i <= n2; i++)
    {
        count = 0;
        for (j = 1; j <= i; j++)
        {
            if (i % j == 0)
            {
                count++;
            }
        }
        if (count == 2)
        {
            printf("%d\n", i);
        }
    }*/

    return 0;
}