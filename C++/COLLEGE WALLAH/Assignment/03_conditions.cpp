#include <iostream>
using namespace std;

int main()
{
    // Write a program which takes the values of length and breadth from user and check if it is a square or not.
    /*int length, breadth;
    cout << "Enter the length and breadth" << endl;
    cin >> length >> breadth;

    if (length == breadth)
    {
        cout << "It is Square";
    }
    else
    {
        cout << "It is Rectangle";
    }*/

    // Write a program to print absolute value of a number entered by the user.
    /*int number;
    cout << "Enter the number" << endl;
    cin >> number;

    if (number < 0)
    {
        cout << "Absolute value is : " << (-1) * number;
    }
    else
    {
        cout << "Absolute value is : " << number;
    }*/

    // Write a program to take input from user for Cost Price (C.P.) and Selling Price(S.P.) and calculate Profit or Loss.
    /*int CP, SP, Profit, Loss;
    cout << "Enter the value of cost-price & selling-price" << endl;
    cin >> CP >> SP;

    Profit = SP - CP;
    Loss = CP - SP;
    (SP > CP) ? cout << "Profit is : " << Profit : cout << "Loss is : " << Loss;*/

    // Write a program to print positive number entered by the user, if user enters a negative number, it is skipped.
    /* int num;
     cout << "Enter a number" << endl;
     cin >> num;
     (num > 0) ? cout << "Number is Positive " << num : cout << "The number is negative and skipped";*/

    // Write a program to calculate marks to grades . Follow the conversion rule as given below
    /*int marks;
    cout << "Enter the Marks" << endl;
    cin >> marks;

    if (marks <= 100 && marks > 90)
    {
        cout << "Grade is A++";
    }
    else if (marks <= 90 && marks > 80)
    {
        cout << "Grade is A";
    }
    else if (marks <= 80 && marks > 70)
    {
        cout << "Grade is B++";
    }
    else if (marks <= 70 && marks > 60)
    {
        cout << "Grade is B";
    }
    else if (marks <= 60 && marks > 50)
    {
        cout << "Grade is C";
    }
    else if (marks <= 50 && marks > 40)
    {
        cout << "Grade is D";
    }
    else if (marks <= 40 && marks > 30)
    {
        cout << "Grade is E";
    }
    else
    {
        cout << "grade is F";
    }*/

    return 0;
}