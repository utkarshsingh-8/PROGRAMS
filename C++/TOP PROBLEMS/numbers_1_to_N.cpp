#include <iostream>
using namespace std;

int main()
{ 
    // PRINT NUMBERS FROM 1 TO N                                     //😵😵😵
    /*int n;
    cout << "Enter the Value of n\n";
    cin >> n;

    cout << "The numbers from 1 to N\n";
    int i = 1;
    for (;;)
    {
        if (i <= n)
        {
            cout << i << " ";
            i++;
        }
        else
        {
            break;
        }
    }*/

    // SUM of NUMBERS FROM 1 TO N                                    //😵😵😵
    /*int num, i = 1, sum = 0;
    cout << "Enter the value of n: ";
    cin >> num;

    while (i <= num) // 1st Method
    {
        sum = sum + i;
        i++;
    }
    // sum = (num / 2.0)*(1 + num); // 2nd Method
    cout << "The sum of numbers from 1 to N is " << sum;*/

    // SUM OF EVEN terms BETWEEN 1 TO N                              //😵😵😵
    /*int num, i = 2, sum = 0;
    cout << "Enter the value of n: ";
    cin >> num;

    while (i <= num)
    {
        sum = sum + i;
        i = i + 2;
    }
    cout << "The sum of Even terms between 1 to N is " << sum;*/
}
